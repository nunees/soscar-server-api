{
    "version": 2,
    "builds":[
        {
            "src": "./src/shared/infra/http/app.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}